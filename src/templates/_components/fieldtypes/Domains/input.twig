<input type="hidden" name="{{ name }}" value="">
{% include '_includes/forms/editableTable' with {
    "id": id,
    "name": name,
    "rows": rows,
    "cols": cols,
    "static": static,
    "addRowLabel": "Add a domain"|t,
    "minRows": field.min,
    "maxRows": field.max
} only %}